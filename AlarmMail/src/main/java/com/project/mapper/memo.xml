<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.mapper.memo">
	
	<!-- 予約メモAll照会 -->
	<select id="SelectAll" parameterType="com.project.entity.MemoEntity">
		select * from Memotable where status=0 order by time desc
	</select>

</mapper>